// COPYRIGHT (C) HARRY CLARK 2025
// HITACHI SUPERH INSTRUCTION DECODER

// THIS FILE PERTAINS TOWARDS THE MODULARISATION AND LAYOUT OF THE OPERAND
// IDENTIFIERS, THEIR RESPECTIVE CHARACTERISTICS, THE CONCATENATION OF TYPES
// AND HELPER FUNCTIONS - ALL PERTAINING TOWARDS THE DSP OPERANDS

// NESTED INCLUDES

#include "instructions/instructionDSP.h"
#include "instructions/operandsDSP.h"
#include "instructions/registerDSP.h"

// ACCESS THE CURRENT BUFFER SIZE FOR EACH DSP OPERAND
// FOLLOW THE SAME DESIGN PATTERN OF THE BASE OPERAND DEFINTION
UNK CATH_DSP_OPERAND_TYPE_GET_BUFFER_SIZE(SH_DSP_OPERAND_TYPE TYPE, const SH_DSP_INSTRUCTION* INSTR)
{
    // ASSIGN THE CALLBACK TO EACH RESPECTIVE TYPE
    SH_DSP_OPERAND_CALLBACK CB;
    CB = DSP_INSTR_CALLBACKS[TYPE];
    
    return CB(INSTR, NULL, 0);
}

UNK CATH_DSP_OPERAND_TYPE_DISASM(SH_DSP_OPERAND_TYPE TYPE, const struct SH_DSP_INSTRUCTION* INSTR, char* DEST)
{
    SH_DSP_OPERAND_CALLBACK CB;
    CB = DSP_INSTR_CALLBACKS[TYPE];
    
    return CB(INSTR, DEST, 64);
}

// WRITE A SINGLE STRING LITERAL TOKEN IN RELATION TO THE CURRENT BUS
// BEING ACCESSED, PRECEDED BY A SEPARATOR IF OTHER TOKENS HAVE ALREADY
// BEEN WRITTEN
// 
// NOTE: DEST MAY SOMETIMES BE NULL, IN THOSE CASES ONLY THE SIZE IS ACCUMULATED
STATIC UNK CATH_DSP_WRITE_BUS_TOKEN(char* DEST, UNK OFFSET, const char* TOKEN)
{
    UNK LENGTH = strlen(TOKEN);
    UNK WRITTEN = 0;

    // ASSUME PADDING TO BEGIN WITH AGAINST A PROVIDED BUFFER
    if(OFFSET > 0)
    {
        if(DEST)
        {
            DEST[OFFSET] = ' ';
            DEST[OFFSET + 1] = ' ';
        }

        WRITTEN += 2;
    }

    if(DEST) memcpy(DEST + OFFSET + WRITTEN, TOKEN, LENGTH);
    WRITTEN += LENGTH;

    return WRITTEN;
}

// DETERMINES ALL OF THE CONURRENT OPERANDS AND APPLIES
// THEM TO THE BUFFER
//
// A SIMILAR DESIGN PATTERN TO THE BASE DISASM OPERAND DEFINITION IS REUSED
// ALBEIT ONLY USED FOR TRACKING PARALLELISED INSTRUCTIONS 
UNK CATH_DSP_INSTRUCTION_DISASM_OPERAND(const SH_DSP_INSTRUCTION* INSTR, char* BUFFER, UNK BUFFER_SIZE)
{
    UNK TOTAL_SIZE = 0;

    // DETERMINE THE CURRENTLY USED SLOTS WITHIN THE DSP ITSELF
    // HELPS WITH DETERMINING THE CONDITIONS OF EACH RESPECTIVE "NTH_BUS"

    const SH_DSP_DESCRIPTOR* DSP_DESC = INSTR->DESCRIPTOR;
    assert(!DSP_DESC->IS_NOP && !DSP_DESC->IS_END);

    // ENCODING SCHEME FOR SINGLE OPERAND PATHS SUCH AS DMA
    // WHEREBY THE ENCOMPASSING OPERANDS ONLY TAKE UP ONE SLOT 
    // WITHIN THE PARALLELISED SLOTS
    if(DSP_DESC->SRC != DSP_OPERAND_NONE && DSP_DESC->DEST == DSP_OPERAND_NONE)
    {
        UNK REMAINING_SIZE = (BUFFER != NULL && BUFFER_SIZE > TOTAL_SIZE) ? (BUFFER_SIZE - TOTAL_SIZE) : 0;
        TOTAL_SIZE += DSP_INSTR_CALLBACKS[DSP_DESC->SRC](INSTR, BUFFER 
                    ? BUFFER + TOTAL_SIZE 
                    : NULL, REMAINING_SIZE);

        if(BUFFER != NULL && TOTAL_SIZE < BUFFER_SIZE) BUFFER[TOTAL_SIZE] = '\0';
        return TOTAL_SIZE;
    }

    // 14/05/26
    // REFACTORED SUCH THAT IT IS ABLE TO PROPERLY TWO-OPERAND PATHS
    //
    // THIS IS PRGAMATIC FOR ENCODINGS SUCH AS MVI WHERE THE BROADER
    // INSTRUCTION FAMILY ENCOMPASSES MULTI-OPERANDS
    if(DSP_DESC->SRC != DSP_OPERAND_NONE && DSP_DESC->WRITE_DEST 
                            && DSP_DESC->DEST != DSP_OPERAND_NONE)
    {
        // ALLOCATE THE REQUIRED SPACE AS PER ALL OF THE OPERAND ENTRIES
        // SRC HAS BEEN ADDED TO PROPERLY ADJUST FOR THIS LENGTH

        UNK SRC_REMAINING = (BUFFER != NULL && BUFFER_SIZE > TOTAL_SIZE) ? (BUFFER_SIZE - TOTAL_SIZE) : 0;
        UNK SRC_SIZE = DSP_INSTR_CALLBACKS[DSP_DESC->SRC](INSTR, BUFFER 
                    ? BUFFER + TOTAL_SIZE 
                    : NULL, SRC_REMAINING);

        TOTAL_SIZE = SRC_SIZE;

        // ONLY EMIT THE SEPARATOR IF THE SOURCE OPERAND PRODUCED OUTPUT
        // CONDITIONS BY WHICH THERE IS AN UNCONDITIONAL STATEMENT
        // WOULD BENEFIT FROM THIS
        if(SRC_SIZE > 0)
        {
            if(BUFFER) { BUFFER[TOTAL_SIZE] = ','; BUFFER[TOTAL_SIZE + 1] = ' '; }
            TOTAL_SIZE += 2;
        }

        // 20/06/26
        // SIMILAR SIZE EVALUATIONS MUCH LIKE THE BASE INSTRUCTION SET
        // FROM THE PREVIOUS COMMITS
        //
        // HELPS TO NEGATE THE POTENTIAL UNDERFLOW ISSUES 
        // PERSISTENCE ACROSS BUFFER ALLOCATIONS
        UNK DEST_REMAINING = (BUFFER != NULL && BUFFER_SIZE > TOTAL_SIZE) ? (BUFFER_SIZE - TOTAL_SIZE) : 0;
        TOTAL_SIZE += DSP_INSTR_CALLBACKS[DSP_DESC->DEST](INSTR, BUFFER 
                    ? BUFFER + TOTAL_SIZE 
                    : NULL, DEST_REMAINING);

        // EMIT THE CONDITIONAL SUFFIX OF A PROVIDED INSTRUCTION 
        // ASSUMING IT IS PRESENT
        // 
        // THIS IS ONCE AGAIN IMPORTANT FOR CASES SUCH AS MVI
        // SUCH THAT WHEN THE UNCONDITIONAL VARIANT IS CALLED, IT IS ABLE
        // TO BE DIFFERENTIATED THROUGH IT'S DISASSEMBLY
        if(INSTR->INSTR_ID == CATH_INSTR_ID_DSP_MVI)
        {
            TOTAL_SIZE += DSP_INSTR_CALLBACKS[DSP_OPERAND_MVI_COND](INSTR, BUFFER 
                    ? BUFFER + TOTAL_SIZE 
                    : NULL, BUFFER ? 64 - TOTAL_SIZE : 0);
        }

        if(BUFFER) BUFFER[TOTAL_SIZE] = '\0';
        return TOTAL_SIZE;

    }

    if(DSP_DESC->X_BUS) TOTAL_SIZE += CATH_DSP_WRITE_BUS_TOKEN(BUFFER, TOTAL_SIZE, "X");
    if(DSP_DESC->D_BUS) TOTAL_SIZE += CATH_DSP_WRITE_BUS_TOKEN(BUFFER, TOTAL_SIZE, "P");
    if(DSP_DESC->Y_BUS) TOTAL_SIZE += CATH_DSP_WRITE_BUS_TOKEN(BUFFER, TOTAL_SIZE, "Y");

    // EMIT THE DESTINATION OPERAND IF THE DESCRIPTOR HAS ONE
    // ROUTES THROUGH THE CALLBACK TABLE SO PC, TOP, IMM ETC
    // ALL RESOLVE TO THEIR CORRECT STRING WITHOUT SPECIAL CASING HERE
    if(DSP_DESC->WRITE_DEST && DSP_DESC->DEST != DSP_OPERAND_NONE)
    {
        SH_DSP_OPERAND_CALLBACK DSP_CB = DSP_INSTR_CALLBACKS[DSP_DESC->DEST];

        if(BUFFER != NULL)
        {
            UNK REMAINING = (BUFFER_SIZE > TOTAL_SIZE) ? (BUFFER_SIZE - TOTAL_SIZE) : 0;
            UNK WRITTEN = DSP_CB(INSTR, BUFFER + TOTAL_SIZE, REMAINING);
            TOTAL_SIZE += WRITTEN;
        }
        
        else
        {
            TOTAL_SIZE += DSP_CB(INSTR, NULL, 0);
        }
    }

    if(BUFFER != NULL && TOTAL_SIZE < BUFFER_SIZE) BUFFER[TOTAL_SIZE] = '\0';
    return TOTAL_SIZE;
}

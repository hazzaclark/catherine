// COPYRIGHT (C) HARRY CLARK 2025
// HITACHI SUPERH INSTRUCTION DECODER

// THIS FILE PERTAINS TOWARDS THE MODULARISATION AND LAYOUT OF THE INSTRUCTIONS
// IDENTIFIERS, THEIR RESPECTIVE CHARACTERISTICS, THE CONCATENATION OF TYPES
// AND HELPER FUNCTIONS - ALL PERTAINING TOWARDS THE INSTRUCTIONS OF THE SCU DSP 

// NESTED INCLUDES

#include "tables/tables.h"
#include "instructions/instructionDSP.h"

// INITIALISE THE BASIS FOR THE PROVIDED INSTRUCTION
// ACCESS THE WORD ENCOMPASSING THE INSTRUCTION AND IT'S RESPECTIVE ADDRESS
void CATH_DSP_INSTRUCTION_INIT(SH_DSP_INSTRUCTION* INSTR, U32 WORD, U32 ADDRESS)
{
    INSTR->WORD = WORD;
    INSTR->FLAGS = 0;
    INSTR->DESCRIPTOR = &DSP_DESCRIPTORS[CATH_INSTR_ID_DSP_INVALID];
    INSTR->INSTR_ID = CATH_INSTR_ID_DSP_INVALID;
    INSTR->CATEGORY = CATH_INSTRCAT_DSP;
    INSTR->PC = ADDRESS;
    INSTR->IS_PARALLEL = false;

    // INITIALISE ALL OF THE SLOTS BASED ON THEIR CORRESPONDENCE
    // HANDLING THEM THIS WAY PREVENTS HAVING TO DO REINIT
    // ON EVERY DECODING OF THE SLOTS - WHICH CAUSED FURTHER ISSUES

    SH_DSP_PARALLEL_SLOT* SLOTS[] = 
    {
        &INSTR->XP_SLOT,
        &INSTR->X_SLOT,
        &INSTR->YA_SLOT,
        &INSTR->Y_SLOT,
        &INSTR->D_SLOT,
    };

    for(UNK INDEX = 0; INDEX < 5; INDEX++)
    {
        SLOTS[INDEX]->IS_ACTIVE = false;
        SLOTS[INDEX]->MNEMONIC = NULL;
        SLOTS[INDEX]->DEST_NAME = NULL;
        SLOTS[INDEX]->SRC_NAME = NULL;
        SLOTS[INDEX]->SOURCE = DSP_OPERAND_NONE;
    }
}

// HOLISTIC DEFINTION FOR EMITTING THE CURRENT SLOT AND 
// IT'S CHARACTERISTICS BASED ON WHAT IS CURRENTLY BEING USED
//
// ACCESSES THE DIRECTIVES TO PROCESS SPECIFIC CORRESPONDENCE
// RELATING TO THAT CURRENT STATE
//
// OTHERWISE, IT WILL JUST PRINT OUT THE FULL WORD WITH IT'S
// RESPECTIVE OPERANDS
STATIC
UNK CATH_DSP_EMIT_SLOT(const SH_DSP_PARALLEL_SLOT* SLOT,
                               const SH_DSP_INSTRUCTION* INSTR,
                               char* BUFFER)
{
    // ALL INACTIVE SLOTS STILL OCCUPY
    // SPACE IN THE PARALLEL WORD REGARDLESS OF IT'S ENCODING
    //
    // ALL FOUR ENTRIES ARE ALWAYS PRESENT IN EVERY 32-BIT WORD
    // THEREFORE, EMIT NOP TO REPRESENT THE ENCODED STUBS FOR THAT OPERATION

    if(!SLOT->IS_ACTIVE)
    {
        if(BUFFER) return snprintf(BUFFER, 64, "NOP");
        return sizeof("NOP") - 1;
    }

    const UNK SIZE = BUFFER ? 64 : 0;

    if(SLOT->SOURCE == DSP_OPERAND_IMM)
    {
        U8 SIMM = (U8)SCU_DSP_GET_D_SIMM(INSTR);
        return snprintf(BUFFER, SIZE,
            "%s #0x%02X, %s",
            SLOT->MNEMONIC, SIMM, SLOT->DEST_NAME);
    }

    if(SLOT->SRC_NAME)
    {
        return snprintf(BUFFER, SIZE,
            "%s %s, %s",
            SLOT->MNEMONIC, SLOT->SRC_NAME, SLOT->DEST_NAME);
    }

    return snprintf(BUFFER, SIZE,
            "%s %s",
            SLOT->MNEMONIC, SLOT->DEST_NAME);
}

// 12/05/26
// THE FOLLOWING FUNCTIONS WILL LOOK TO LEVERAGE
// THE INITIALISATION OF ALL THREE SLOTS BASED ON THEIR STATE
// 
// THROUGH THIS, MORE CONDUCIVE RESULTS OF WHICH ENTRY IS CURRENTLY
// BEING READ IN RELATION TO IT'S CORRESPONDENCE

// DETERMINE THE SIZE OF THE CURRENT INSTRUCTION IN THE OUTPUT BUFFER
// WITHOUT WRITING TO IT ACCORDINGLY
//
// MIRRORS THE DESIGN PATTERNS OF CATH_INSTRUCTION_GET_SIZE 
// USED BY CALLERS THAT NEED TO ALLOCATE BEFORE WRITING
UNK CATH_DSP_INSTRUCTION_GET_SIZE(const SH_DSP_INSTRUCTION* INSTR)
{
    UNK TOTAL_SIZE = 0;
    TOTAL_SIZE += strlen(CATH_GET_DSP_OPCODE_NAME(INSTR->INSTR_ID));

    if(INSTR->DESCRIPTOR->IS_NOP || INSTR->DESCRIPTOR->IS_END)
    {
        if(INSTR->IS_PARALLEL)
        {
            SCU_DSP_CREATE_SLOTS(INSTR, SLOTS);
            for(UNK INDEX = 0; INDEX < SCU_DSP_SLOT_COUNT; INDEX++)
            {
                TOTAL_SIZE += 2;
                TOTAL_SIZE += CATH_DSP_EMIT_SLOT(SLOTS[INDEX], INSTR, NULL);
            }
        }

        return TOTAL_SIZE;
    }

    TOTAL_SIZE += 1;
    TOTAL_SIZE += CATH_DSP_INSTRUCTION_DISASM_OPERAND(INSTR, NULL);

    if(INSTR->IS_PARALLEL)
    {
        SCU_DSP_CREATE_SLOTS(INSTR, SLOTS);
        for(UNK INDEX = 0; INDEX < SCU_DSP_SLOT_COUNT; INDEX++)
        {
            TOTAL_SIZE += 2;
            TOTAL_SIZE += CATH_DSP_EMIT_SLOT(SLOTS[INDEX], INSTR, NULL);
        }
    }

    return TOTAL_SIZE;
}

UNK CATH_DSP_INSTRUCTION_DISASM(const SH_DSP_INSTRUCTION* INSTR, char* DEST)
{
    UNK TOTAL_SIZE = 0;
    char* BUFFER = DEST;

    const char* DSP_INSTR = CATH_GET_DSP_OPCODE_NAME(INSTR->INSTR_ID);
    UNK DSP_INSTR_LEN = strlen(DSP_INSTR);

    if(BUFFER)
    {
        memcpy(BUFFER, DSP_INSTR, DSP_INSTR_LEN);
        BUFFER += DSP_INSTR_LEN;
    }

    TOTAL_SIZE += DSP_INSTR_LEN;

    // FOR THOSE INSTRUCTIONS THAT ARE NON-PARALLEL
    // EMIT THE OPERANDS DIRECTLY AFTER THE MNEMONIC
    if(!INSTR->IS_PARALLEL && !INSTR->DESCRIPTOR->IS_NOP && !INSTR->DESCRIPTOR->IS_END)
    {
        if(BUFFER) { BUFFER[0] = ' '; BUFFER += 1; }
        TOTAL_SIZE += 1;

        UNK WRITTEN = CATH_DSP_INSTRUCTION_DISASM_OPERAND(INSTR, BUFFER);
        if(BUFFER) BUFFER += WRITTEN;
        TOTAL_SIZE += WRITTEN;
    }

    // FOR THOSE INSTRUCTIONS BY WHICH THE LENGTHS OF THE 
    // COMBINATORIAL INSTRUCTION PATTERNS INVOLVE 
    // THE CORRESPONDENCE OF THE CURRENT ENTRY SLOT
    //
    // ASSUME THEN THAT ONCE A SLOT HAS BEEN FOUND
    // ALLOCATE THE PROVIDED BUFFER SPACE AND EMIT IT'S CONTENTS
    
    if(INSTR->IS_PARALLEL)
    {
        SCU_DSP_CREATE_SLOTS(INSTR, SLOTS);
        for(UNK INDEX = 0; INDEX < SCU_DSP_SLOT_COUNT; INDEX++)
        {
            if(BUFFER) { BUFFER[0] = ' '; BUFFER[1] = ' '; BUFFER += 2; }
            TOTAL_SIZE += 2;

            UNK WRITTEN = CATH_DSP_EMIT_SLOT(SLOTS[INDEX], INSTR, BUFFER);
            if(BUFFER) BUFFER += WRITTEN;
            TOTAL_SIZE += WRITTEN;
        }
    }

    if(BUFFER) *BUFFER = '\0';
    return TOTAL_SIZE;
}
